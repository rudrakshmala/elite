- [ ] Add /api/health-debug endpoint to app.py (no auth) to detect whether Render/proxy is blocking /api/* (returns 200)
- [ ] Ensure code formatting + no unintended behavioral changes
- [ ] Run local server and curl /api/health-debug + existing endpoints to confirm
- [ ] Build frontend and verify VITE_API_URL is correct (optional if needed)
- [ ] Update changelog/result

